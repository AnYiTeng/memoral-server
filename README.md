## memoral-server

Node.js + Express + MySQL 墓碑二维码项目后端 API。

### 环境变量示例（.env）

```bash
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=tomb_qr_code
PORT=3001
```

### 启动项目

```bash
npm install
npm start
```
